import { ComingSoonPanel } from "@/components/ComingSoonPanel";

export default function StaysPage() {
  return (
    <ComingSoonPanel
      title="Stays & hotels"
      blurb="Hotel and stay search is on the way. For now, find your flights — stays will land here next."
    />
  );
}
